# VoiceReply Environment Variables

# Gemini API Key (get from https://aistudio.google.com/app/apikey)
# This is used as fallback if users don't set their own API key
GEMINI_API_KEY=your_api_key_here
